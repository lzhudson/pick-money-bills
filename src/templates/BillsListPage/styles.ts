import styled from 'styled-components'

export const ContractBillsContainer = styled.div`
  max-width: calc(1060px + 40px);
  margin: 0 auto;
  margin-top: 50px;
  padding: 0 20px;
  @media (max-width: 1024px) {
    padding: 0 20px;
  }
`
